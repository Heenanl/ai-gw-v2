<!--
    - Policies are applied in the order they appear.
    - Position <base/> inside a section to inherit policies from the outer scope.
    - Comments within policies are not preserved.
-->
<!-- Add policies as children to the <inbound>, <outbound>, <backend>, and <on-error> elements -->
<policies>
    <!-- Throttle, authorize, validate, cache, or transform the requests -->
    <inbound>
        <base />
        <!-- Extract deployment name from URL path (AOAI format) or request body model field (v1 format) -->
        <set-variable name="productDeploymentName" value="@{
            try {
                var path = context.Request.Url.Path;
                var segments = path.Split(new[] { '/' }, StringSplitOptions.RemoveEmptyEntries);
                for (int i = 0; i &lt; segments.Length - 1; i++) {
                    if (segments[i] == &quot;deployments&quot;) {
                        return System.Uri.UnescapeDataString(segments[i + 1]);
                    }
                }
                var body = context.Request.Body.As&lt;JObject&gt;(preserveContent: true);
                if (body != null &amp;&amp; body[&quot;model&quot;] != null) {
                    return body[&quot;model&quot;].ToString();
                }
            } catch {}
            return &quot;unknown&quot;;
        }" />
        <!-- Capacity management: per-subscription, per-model token quota -->
        <llm-token-limit
            counter-key="@(context.Subscription.Id + &quot;:&quot; + (string)context.Variables[&quot;productDeploymentName&quot;])"
            tokens-per-minute="400"
            estimate-prompt-tokens="false"
            tokens-consumed-header-name="x-ratelimit-consumed-tokens"
            remaining-tokens-header-name="x-ratelimit-remaining-tokens"
            token-quota="100000"
            token-quota-period="Monthly"
            retry-after-header-name="retry-after" />
    </inbound>
    <!-- Control if and how the requests are forwarded to services  -->
    <backend>
        <base />
    </backend>
    <!-- Customize the responses -->
    <outbound>
        <base />
    </outbound>
    <!-- Handle exceptions and customize error responses  -->
    <on-error>
        <base />
        <!-- Raising throttling events (http 429 only) can help in setting up alerts in App Insights -->
        <!-- Set the following variables to customize the throttling event details -->
        <!-- <set-variable name="productName" value="@(context.Product?.Name?.ToString() ?? "Portal-Admin")" />
        <set-variable name="deploymentName" value="@((string)context.Variables.GetValueOrDefault<string>("requestedModel", "DefaultModel"))" />
        <set-variable name="appId" value="@((string)context.Variables.GetValueOrDefault<string>("appId", context.Subscription?.Id ?? "Portal-Admin-Sub"))" />
        <include-fragment fragment-id="raise-throttling-events" /> -->
    </on-error>
</policies>